# WoaScanAgent
## Prerequisites


## How to Run
1. You need to have rust installed to compile this. Follow instructions from [here](https://www.rust-lang.org/tools/install).
2. Clone the repository:
    ```bash
    git clone https://github.com/BearBrix/WoaScanAgent.git
    cd WoaScanAgent
    ```

3. Build the project:
    ```bash
    cargo build --release
    ```

4. Run the application:
    ```bash
    cargo run
    ```
